# NPM Configuration for ISO Easy Web
# Handle peer dependency conflicts gracefully
legacy-peer-deps=true

# Use npm registry
registry=https://registry.npmjs.org/

# Save exact versions for better reproducibility
save-exact=true
